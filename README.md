# Spring-Clound-Gateway
git init
git add .
git commit -m "chore: initial commit"

Chọn 1 trong 2 cách: SSH hoặc HTTPS

SSH (khuyến nghị)
git remote add origin git@github.com:<username-or-org>/<repo>.git


Push lần đầu & đặt upstream
git push -u origin main
